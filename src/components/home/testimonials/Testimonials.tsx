import scss from "./Testimonials.module.scss"

const Testimonials = () => {
    return (
        <section id={scss.Testimonials}>
            
        </section>
    );
};

export default Testimonials;